on:
  push:
    branches:
      - main

jobs:
  build_css:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source Git branch
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 1

      - name: Make destination directory for compiled CSS
        run: mkdir -vp /tmp/website/assets/css

      - name: Compile CSS from SCSS files
        uses: gha-utilities/sass-build@v0.3.7
        with:
          source: |
            assets/sass/main.scss
            assets/sass/noscript.scss
          destination: |
            /tmp/website/assets/css/main.css
            /tmp/website/assets/css/noscript.css

      - name: Checkout destination Git branch
        uses: actions/checkout@v2
        with:
          ref: gh-pages
          fetch-depth: 1

      - name: Move compiled CSS to path within pr-pages branch
        run: mv /tmp/website/assets/css assets/

      - name: Add and Commit changes to pr-pages branch
        run: |
          git config --local user.email 'action@github.com'
          git config --local user.name 'GitHub Action'
          git add -f assets/css/* 
          git commit -m 'Updates compiled CSS files'

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
